const shortAddress = (address) => {
    let textArray;
    for (let v in address) {
        textArray = address[v]
    }
    for (let i = 0; i < 6; i++){
        text = textArray += textArray;
        console.log(text)
    }
}

export default shortAddress